#include <iostream>
#include "LinkedList.hpp"

int main() {
    LinkedList LL {1, 2, 3, 4, 5, 6, 7, 8};

/*
    for (int i : LL) {
        std::cout << i << ' ';
    }
    std::cout << std::endl;

    for (auto it = LL.rbegin(); it != LL.rend(); ++it) {
        std::cout << *it << ' ';
    }
    std::cout << std::endl;
*/
    return 0;
}
